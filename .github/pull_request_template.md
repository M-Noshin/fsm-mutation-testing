## What this changes

## Checklist

- [ ] `pytest` passes
- [ ] If `core.py`, `io.py` or an algorithm changed: independent replay
      verification run and every row reports `ok`
- [ ] If a column's meaning changed: `docs/METHODOLOGY.md` updated
- [ ] No new kill-detection logic outside `core.step`

## Effect on published numbers

Does this change any figure already reported? If so, which, and by how much?
